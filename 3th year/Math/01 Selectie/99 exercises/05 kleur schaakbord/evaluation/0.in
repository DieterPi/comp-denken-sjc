>>> kleur( (1, 1) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (2, 6) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (8, 7) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (5, 8) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (7, 6) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (7, 8) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (4, 1) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (5, 3) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (1, 3) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (6, 8) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (5, 2) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (5, 6) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (7, 1) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (1, 2) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (7, 2) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (1, 5) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (8, 7) ) # doctest: +STDOUT
Een licht gekleurd veld
>>> kleur( (2, 8) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (1, 5) ) # doctest: +STDOUT
Een donker gekleurd veld
>>> kleur( (5, 4) ) # doctest: +STDOUT
Een licht gekleurd veld
